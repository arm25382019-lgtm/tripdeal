# TripDeal

TripDeal is a Thailand-focused travel deal finder.

## Stack
- GitHub — source of truth
- Codex — development
- Supabase — database and auth
- Vercel — preview and production

> This public repository must never contain secrets, API keys, service-role keys, or `.env` files.
